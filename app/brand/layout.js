export const metadata = {
  title: 'Brand Identity System | chuck design',
  description: 'Brand guidelines, color system, typography, and visual direction for chuck design.',
  robots: 'noindex',
};

export default function BrandLayout({ children }) {
  return children;
}
